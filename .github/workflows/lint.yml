name: ShellCheck

on:
  push:

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install ShellCheck
        run: |
          sudo apt update
          sudo apt install shellcheck -y

      - name: Run ShellCheck
        run: |
          shellcheck *.sh
